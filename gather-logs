#!/bin/bash

readonly ExecAbsPath=$(readlink --canonicalize "$0")
readonly ExecDir=$(dirname "$ExecAbsPath")

readonly LogDir=/var/lib/irods/iRODS/server/log


main()
{
  local host="$1"
  local password="$2"

  for log in $(list_logs "$host" "$password")
  do
    local logName
    logName=$(basename "$log")

    rcat_log "$host" "$password" "$log" \
      | "$ExecDir"/format-log-entrhost --assign YEAR="${logName:8:4}"

    printf 'gather_logs:  finished processing %s\n' "$logName" >&2
  done
}


list_logs()
{
  local host="$1"
  local password="$2"

  ssh -q -t "$host" <<EOF 2> /dev/null
printf '%s\n' "$password" | sudo -S find "$LogDir" -maxdepth 1 -name rodsLog.* -type f | sort
EOF
}


rcat_log()
{
  local host="$1"
  local password="$2"
  local log="$3"

  ssh -q -t "$host" <<EOF 2> /dev/null
printf '%s\n' "$password" | sudo -S cat "$log"
EOF
}


main "$@"
